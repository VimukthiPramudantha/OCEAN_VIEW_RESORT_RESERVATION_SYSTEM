<?xml version="1.0" encoding="UTF-8"?>
<Context>

    <!--
        Tomcat JDBC Connection Pool (built-in since Tomcat 7+)
        Looked up via JNDI name: java:comp/env/jdbc/hotelDB
    -->
    <Resource name="jdbc/hotelDB"
              auth="Container"
              type="javax.sql.DataSource"
              factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"

              driverClassName="com.mysql.cj.jdbc.Driver"
              url="jdbc:mysql://localhost:3306/hoteldb?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true&amp;useUnicode=true&amp;characterEncoding=UTF-8"

              username="root"
              password="root123"

              maxTotal="50"           <!-- max active connections -->
    maxIdle="10"            <!-- max idle connections kept open -->
    minIdle="5"
    initialSize="5"
    maxWaitMillis="10000"   <!-- wait max 10s for connection -->

    testOnBorrow="true"
    validationQuery="SELECT 1"
    validationInterval="30000"

    timeBetweenEvictionRunsMillis="30000"
    minEvictableIdleTimeMillis="60000"

    logAbandoned="true"
    removeAbandoned="true"
    removeAbandonedTimeout="60"
    />

</Context>